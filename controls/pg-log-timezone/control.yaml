id: pg-log-timezone
name: Ensure log_timezone is set correctly
description: >
  Verify that log_timezone is set to a known timezone for consistent log timestamps.
severity: low
engine:
  - name: postgresql
    versions: ["14", "15", "16"]
frameworks:
  - id: cis-postgresql-16
    section: "3.1.26"
    title: "Ensure log_timezone is set correctly"
tags: ['logging', 'audit']
requires_auth: true
