id: pg-set-user-extension
name: Ensure the set_user extension is installed
description: >
  Verify that the set_user extension is installed for privilege escalation control.
severity: medium
engine:
  - name: postgresql
    versions: ["14", "15", "16"]
frameworks:
  - id: cis-postgresql-16
    section: "4.8"
    title: "Ensure the set_user extension is installed"
tags: ['access-control', 'privilege-escalation']
requires_auth: true
