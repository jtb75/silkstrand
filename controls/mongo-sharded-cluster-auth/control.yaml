id: mongo-sharded-cluster-auth
name: Ensure authentication is enabled in the sharded cluster
description: >
  CIS MongoDB 8 Benchmark control 2.3.
severity: high
engine:
  - name: mongodb
    versions: ["6", "7", "8"]
frameworks:
  - id: cis-mongodb-8
    section: "2.3"
    title: "Ensure authentication is enabled in the sharded cluster"
tags: ['authentication', 'access-control', 'cluster']
requires_auth: true
